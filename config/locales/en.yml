# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  errors:
    messages:
      wrong_size: "is the wrong size (should be %{file_size})"
      size_too_small: "is too small (should be at least %{file_size})"
      size_too_big: "is too big (should be at most %{file_size})"
  views:
    pagination:
      previous: "Prev"
      next: "Next"
    two_factor_auths:
      new:
        already_enabled_otp: "You've already enabled two-factor authentication using mobile authenticator applications."
    accounts:
      show:
        disable_two_factor_confirm: "Are you sure? This will invalidate your registered applications and U2F devices."
